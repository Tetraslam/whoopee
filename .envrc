# Optional direnv: activate the root virtualenv when you cd in.
# `direnv allow` once to enable. Falls back gracefully if .venv isn't built yet.
export VIRTUAL_ENV="$PWD/.venv"
[ -d "$VIRTUAL_ENV" ] && PATH_add "$VIRTUAL_ENV/bin"
