{
  "name": "whoopee",
  "version": "0.1.0",
  "private": true,
  "description": "WHOOP playground root. TS/JS projects live under projects/, shared libs under lib/ts.",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "lint": "oxlint",
    "format": "prettier --write ."
  }
}
